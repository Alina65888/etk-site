pages:
  stage: deploy
  image: alpine:latest
  script:
    - mkdir -p public
    - cp index.html admin.html etk-shared.js public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
